---
name: article-cat-illustrations
description: 生成「简笔画三花猫」风格的中文正文配图。用于用户要求为中文文章、帖子、博客、Notion 文档、工作流文档、方法论、流程、结构、状态、隐喻或观点生成"怪诞""三花猫""简笔画""正文配图""文章插图""配图建议""shot list""去标题/改图"等任务；默认使用简笔画三花猫 IP（黑白线稿+红蓝橙手写批注，极简留白，禁止写实），生图通过 qiyuan-image 技能（qwen-image API）完成。
---

# 三花猫怪诞正文配图

## 核心定位

为中文文章设计和生成 16:9 横版正文配图。目标不是做商业插画、PPT 信息图或可爱卡通，而是把文章里的关键判断、流程、结构、状态或隐喻，变成一张清爽、怪诞、有创意、可读但不说明书的手绘解释图。

默认视觉 IP 是"简笔画三花猫"：与原版小黑同级的简笔画角色——细黑线空心身体、小黑点眼、尖耳、火柴棍腿、冷淡脸，认真做一件荒诞但成立的事。**不是写实猫、不是动物插画**，见 `references/cat-ip.md`。猫必须参与画面的核心动作，不能只是站在旁边当装饰。

## 生图方式

本 skill 的生图统一通过 **qiyuan-image 技能**（qwen-image / qwen-image-edit API）完成，不使用 `image_gen`。

- CLI 位置：`~/.codex/skills/qiyuan-image/qiyuan-image.mjs`（API key 从 `QIYUAN_API_KEY` 或 `~/.codex/qiyuan.env` 读取，不要把 key 写进仓库）。
- 文生图：

  ```bash
  node ~/.codex/skills/qiyuan-image/qiyuan-image.mjs generate \
    --prompt "<references/prompt-template.md 中的完整提示词>" \
    --out assets/<article-slug>-illustrations/01-topic-name.png \
    --size 1664x928
  ```

- 角色一致性 / 局部修改用图生图（把真实猫照作为参考图）：

  ```bash
  node ~/.codex/skills/qiyuan-image/qiyuan-image.mjs edit \
    --image <skill-dir>/assets/cat/cat-01.png \
    --prompt "<提示词：把图中的猫保持原样，替换成目标构图……>" \
    --out <目标文件>.png \
    --strength 0.6
  ```

- 尺寸：16:9 横版，优先 `1664x928`；API 不支持时退化为 `1344x768` 或 `1024x576`。

### 角色一致性策略

1. 每张图先按 `references/prompt-template.md` 的中文模板文生图。
2. 画风跑偏成写实猫/素描/阴影时，用 `edit` 命令执行模板里的"风格跑偏成写实时"指令，把整图拉回极简黑白线稿（比整图重生成更能保住构图）。
3. 只改局部（去标题、去错字）时优先用 `edit` 做局部编辑。
4. `assets/cat/` 真实猫照只用来确认"这是哪只猫"（三花、白嘴周、尖耳），**不参考其质感和比例**；不要用 edit 直接以猫照为底图，否则画风必然写实。

## 先读这些参考

按任务需要读取，不要一次塞满上下文：

- `references/style-dna.md`：风格 DNA、颜色、文字、禁忌。
- `references/cat-ip.md`：三花猫 IP 的形象、性格、动作库和禁忌。
- `references/composition-patterns.md`：结构类型、原创隐喻方法和反复刻规则。
- `references/prompt-template.md`：单张生图提示词模板（含 qiyuan 命令示例）。
- `references/qa-checklist.md`：生成后检查和迭代规则。
- `assets/examples/`：只作低频视觉校准，不进入默认生成路径。不要照抄这些案例的构图、物件或标注。

## 工作流

### 1. 消化正文

先读用户给的正文、链接、Notion 页面、Markdown 文件或截图内容。提炼：

- 核心观点是什么
- 哪些段落承担认知转折
- 哪些内容适合用图解释
- 哪些地方只适合文字，不需要图

不要平均配图。优先选择"认知锚点"，例如：核心判断、两个断点、输入输出闭环、分流、前后对比、一鱼多吃、承接路径、常见坑、角色状态变化。

### 2. 先出配图策略

如果用户只是说"分析怎么配图 / 思考哪些地方需要配图"，先给 shot list。每张图写清楚：

- 放在哪个段落后
- 图的主题
- 核心意思
- 结构类型
- 三花猫在图里做什么
- 建议元素
- 建议中文标注词

默认 4-8 张。文章很短时 1-3 张；长文也不要轻易超过 9 张。够用就好，避免把正文做成画册。

### 3. 单张生成

如果用户明确要求"生成 / 输出 / 做图 / 帮我生成"，不要停下来等确认；用 qiyuan-image 每张单独生成。不要把多张图拼在一张里。

每张图只讲一个核心结构。提示词必须包含：

- 16:9 横版中文正文配图
- 纯白背景
- 极简黑白线稿（圆珠笔手绘感，角色身体内部留白不填充，仅猫背一小块黑色色块表示三花）
- 少量红色/橙色/蓝色中文手写批注
- 大量留白
- 简笔画三花猫作为核心动作主体（空心线稿身体、小黑点眼、火柴棍腿，与原版小黑同级的简笔画角色，禁止写实）
- 禁止 PPT、商业插画、幼稚可爱、复杂架构、左上角类型标题

不要复刻过往案例。案例只提供风格密度和猫的参与方式，不能直接复用已有构图，除非用户明确要求复刻某张图。每次都要从当前文章重新发明一个奇怪但成立的隐喻。

### 4. 检查与迭代

生成后检查 `references/qa-checklist.md`。如果出现以下问题，优先用 qiyuan `edit` 局部修改或重生成：

- 猫只是装饰
- 猫被画成写实动物（毛发纹理、体积感、绿眼睛、摄影质感）
- 不是简笔画猫（身体没留白、线条不是细黑线、不是火柴棍腿）
- 中文标注连笔、重叠、糊在一起
- 画面太满
- 太像流程图/PPT
- 中文太多或错字严重
- 左上角出现"常见坑/流程图/系统架构图"等标题
- 画风太可爱、幼稚、死板
- 出现黑/红/蓝/橙之外的颜色
- 背景不是干净白底

### 5. 保存交付

如果用户在 workspace 内工作，把最终图复制到：

```text
assets/<article-slug>-illustrations/
```

按顺序命名：

```text
01-topic-name.png
02-topic-name.png
```

保留原始生成文件，不要覆盖已有资产，除非用户明确要求替换。

## 输出口径

生成前的策略输出要短而准。生成后的交付要包含：

- 生成了几张
- 每张图的用途
- 保存路径
- 哪些图最稳，哪些图是可选

不要长篇解释风格理论；让图自己说话。
